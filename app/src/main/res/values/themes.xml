<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.HostelMate" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:fontFamily">@font/font_family_resource</item>
        <item name="textSize">12sp</item>
       <item name="android:windowBackground">@color/background_bg</item>
        <item name="android:textColor">@color/primary_txt_color</item>

    </style>
    
    <style name="Theme.App.Splashscreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_screen_img</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.HostelMate</item>
    </style>

    <style name="circleImageView" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>
    <style name="roundedCoverImageView" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">45dp</item>
    </style>

    <style name="Theme.HostelMate" parent="Base.Theme.HostelMate" />
</resources>